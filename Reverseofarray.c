#include<stdio.h>
int main() {
    int arr[] = {1,4,7,8,3};
    int size,i,arr[size];
    for(int i = 0; i < arr[size] / 2; i++){
    int temp;
    int arr[i] = arr[size - i - 1];
    int temp = arr[i];
    int temp = arr[size - i - 1];
    }
    printf("Reverse of given array = %d\n");
    return 0;
}